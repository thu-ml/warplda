#pragma once

#include <vector>
#include <utility>
#include <string>

//using Int = int;
//using VI = std::vector<Int>;
//using PII = std::pair<Int, Int>;
//using VII = std::vector<PII>;

#include <cstdint>

using TUID = uint32_t;// document id
using TVID = uint32_t;// word id
using TEID = uint64_t;// number of tokens
using TDegree = uint32_t; //num of degree of vertex
using TTopic = uint32_t;//k
using TCount = uint32_t;//ck[k]
